#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - script Prints all single numbers of base 10.
* Return: 0
*/

int main(void)
{
int num;

for (num = 50 ; num < 78 ; num++)
{
putchar(num);
}
putchar(10);
return (0);
}
